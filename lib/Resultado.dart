import 'package:flutter/material.dart';

// ignore: camel_case_types
class Resultado extends StatelessWidget {
  const Resultado({super.key, required String altura});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}